import { View, Text } from "react-native";

const SavingsScreen = () => (
    <View className="flex-1 justify-center items-center">
      <Text className="text-lg font-bold">Savings Screen</Text>
    </View>
  );

export default SavingsScreen;